name: CI

on:
  workflow_call:

jobs:

  terraform-checks:
    if: false
    name: Terraform
    strategy:
      matrix:
        module:
          - ambient
    uses: canonical/charmed-kubeflow-workflows/.github/workflows/terraform-checks-solution.yaml@main
    with:
      module-path: terraform-refactoring/components/${{ matrix.module }}
